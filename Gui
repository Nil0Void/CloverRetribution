local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
local FILE_NAME = "MasterOrbitalConfig.json"

-- Action Remotes Setup
local remote = ReplicatedStorage
    :WaitForChild("ReplicatedPackage")
    :WaitForChild("Remotes")
    :WaitForChild("action")

-- Master State Management
local FARMING_ACTIVE = false
local CHEST_ACTIVE = false
local EQUIP_ACTIVE = false
local SPELL_ACTIVE = false
local DRAGON_BALL_ACTIVE = false

local currentOffset = Vector3.new(0, 5, 15)
local currentTarget = nil
local flyAnchor = nil

-- Cache Queue Framework Memory Assemblies
local activeChestsCache = {}
local activeDragonBallsCache = {}
local chestVisitTimers = {}
local chestVisitCounts = {}
local currentChestIndex = nil
local processedChests = {}

-- Comprehensive Target Dictionary Mapping Parameters
local targets = {
    Mobs = {
        ["Yeti"] = "OFF", ["Vampire Soldier"] = "OFF", ["Stone Guardian"] = "OFF",
        ["Shadow Hand Goon"] = "OFF", ["Seasoned Mercenary"] = "OFF", ["Riddled Skeleton Soldier"] = "OFF",
        ["Pumpkin"] = "OFF", ["Wolf"] = "OFF", ["Mage Apprentice"] = "OFF",
        ["Lesser Devil Juggernaut"] = "OFF", ["Desert Claw"] = "OFF", ["Bandit"] = "OFF",
        ["Wolf Hunter"] = "OFF", ["Wind Monk"] = "OFF", ["Lowly Goblin"] = "OFF",
        ["Werewolf Soldier"] = "OFF"
    },
    Bosses = {
        ["Vennum"] = "OFF", ["Alpha Wolf"] = "OFF", ["Warlord"] = "OFF",
        ["Terra"] = "OFF", ["Red"] = "OFF", ["Alpha Desert Claw"] = "OFF",
        ["Apex"] = "OFF", ["Kraken"] = "OFF", ["The Crimson Witch"] = "OFF",
        ["Netherwing"] = "OFF", ["Khamsin"] = "OFF", ["Ice Golem"] = "OFF",
        ["Nyxar"] = "OFF", ["Word Devil"] = "OFF", ["Devil"] = "OFF",
        ["Medusa"] = "OFF", ["Aetherion"] = "OFF", ["Morningstar"] = "OFF",
        ["Primal Werewolf"] = "OFF", ["Elder Vampire"] = "OFF", ["Vampire Elite"] = "OFF"
    }
}

-- Hand-Mapped Precise Spawn Directory
local spawnMap = {
    ["Vennum"] = "VennumSpawn", ["Alpha Wolf"] = "AlphaWolfSpawn", ["Warlord"] = "AlphaWolfSpawn",
    ["Terra"] = "StoneGuardian1", ["Red"] = "VampireSoldierSpawn", ["Alpha Desert Claw"] = "DesertClaw1",
    ["Apex"] = "AlphaWolfSpawn", ["Kraken"] = "Desert Kraken", ["The Crimson Witch"] = "WindMageSpawn",
    ["Netherwing"] = "LesserDevilSpawn", ["Khamsin"] = "DesertClaw1", ["Ice Golem"] = "YettiSpawn1",
    ["Nyxar"] = "DarkSkeleton", ["Word Devil"] = "LesserDevilSpawn", ["Devil"] = "LesserDevilSpawn",
    ["Medusa"] = "OgreSpawn", ["Aetherion"] = "LesserDevilSpawn", ["Morningstar"] = "LesserDevilSpawn",
    ["Primal Werewolf"] = "WerewolfSoldierSpawn", ["Elder Vampire"] = "VampireSoldierSpawn",
    ["Yeti"] = "YettiSpawn1", ["Vampire Soldier"] = "VampireSoldierSpawn", ["Stone Guardian"] = "StoneGuardian1",
    ["Shadow Hand Goon"] = "ShadowHandSpawn1", ["Seasoned Mercenary"] = "SeasonedMercenarySpawn",
    ["Riddled Skeleton Soldier"] = "DarkSkeleton", ["Pumpkin"] = "Pumpkins", ["Wolf"] = "WoldSpawn",
    ["Mage Apprentice"] = "FireMageSpawn", ["Lesser Devil Juggernaut"] = "LesserDevilSpawn",
    ["Desert Claw"] = "DesertClaw1", ["Bandit"] = "Bandit2Spawn", ["Wolf Hunter"] = "WoldSpawn",
    ["Wind Monk"] = "WindMageSpawn", ["Lowly Goblin"] = "LowlyGoblinn", ["Werewolf Soldier"] = "WerewolfSoldierSpawn"
}

-- Data Profile Storage Logic
local function saveConfig()
    if not writefile then return end
    pcall(function()
        writefile(FILE_NAME, HttpService:JSONEncode({
            FARMING_ACTIVE = FARMING_ACTIVE,
            CHEST_ACTIVE = CHEST_ACTIVE,
            EQUIP_ACTIVE = EQUIP_ACTIVE,
            SPELL_ACTIVE = SPELL_ACTIVE,
            DRAGON_BALL_ACTIVE = DRAGON_BALL_ACTIVE,
            currentOffset = {currentOffset.X, currentOffset.Y, currentOffset.Z},
            targets = targets
        }))
    end)
end

local function loadConfig()
    pcall(function()
        if readfile and isfile and isfile(FILE_NAME) then
            local data = HttpService:JSONDecode(readfile(FILE_NAME))
            if data then
                FARMING_ACTIVE = data.FARMING_ACTIVE or false
                CHEST_ACTIVE = data.CHEST_ACTIVE or false
                EQUIP_ACTIVE = data.EQUIP_ACTIVE or false
                SPELL_ACTIVE = data.SPELL_ACTIVE or false
                DRAGON_BALL_ACTIVE = data.DRAGON_BALL_ACTIVE or false
                
                if data.currentOffset and type(data.currentOffset) == "table" and #data.currentOffset >= 3 then
                    currentOffset = Vector3.new(
                        tonumber(data.currentOffset[1]) or 0, 
                        tonumber(data.currentOffset[2]) or 5, 
                        tonumber(data.currentOffset[3]) or 15
                    )
                else
                    currentOffset = Vector3.new(0, 5, 15)
                end
                
                if data.targets then
                    if data.targets.Mobs then
                        for k, v in pairs(data.targets.Mobs) do if targets.Mobs[k] then targets.Mobs[k] = tostring(v) end end
                    end
                    if data.targets.Bosses then
                        for k, v in pairs(data.targets.Bosses) do if targets.Bosses[k] then targets.Bosses[k] = tostring(v) end end
                    end
                end
            end
        end
    end)
end
loadConfig()

-- Core Canvas Setup
local gui = Instance.new("ScreenGui")
gui.Name = "MasterOrbitalFarmingGui"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 440, 0, 560)
frame.Position = UDim2.new(0.5, -220, 0.5, -280)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.BorderSizePixel = 2
frame.BorderColor3 = Color3.fromRGB(60, 60, 60)
frame.Active = true
frame.Parent = gui

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(1, 0, 0, 35)
titleLabel.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
titleLabel.Text = "📐 Master Priority Automation Controller"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.TextSize = 16
titleLabel.Parent = frame

-- Toggle Utility Creation Factory
local function buildSystemButton(textOn, textOff, xPos, yPos, varName, clickCallback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 135, 0, 32)
    btn.Position = UDim2.new(0, xPos, 0, yPos)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 13
    btn.Parent = frame

    local function refresh()
        local liveState = false
        if varName == "FARMING_ACTIVE" then liveState = FARMING_ACTIVE
        elseif varName == "CHEST_ACTIVE" then liveState = CHEST_ACTIVE
        elseif varName == "EQUIP_ACTIVE" then liveState = EQUIP_ACTIVE
        elseif varName == "SPELL_ACTIVE" then liveState = SPELL_ACTIVE
        elseif varName == "DRAGON_BALL_ACTIVE" then liveState = DRAGON_BALL_ACTIVE end

        btn.Text = liveState and textOn or textOff
        btn.BackgroundColor3 = liveState and Color3.fromRGB(46, 117, 89) or Color3.fromRGB(55, 55, 55)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    end
    refresh()

    btn.MouseButton1Click:Connect(function()
        clickCallback()
        refresh()
        saveConfig()
    end)
end

-- Render Row 1 Controls
buildSystemButton("Auto Farm: ON", "Auto Farm: OFF", 10, 45, "FARMING_ACTIVE", function() FARMING_ACTIVE = not FARMING_ACTIVE end)
buildSystemButton("👑 Chests: ON", "👑 Chests: OFF", 152, 45, "CHEST_ACTIVE", function() CHEST_ACTIVE = not CHEST_ACTIVE end)
buildSystemButton("🐉 Dragon Ball: ON", "🐉 Dragon Ball: OFF", 295, 45, "DRAGON_BALL_ACTIVE", function() DRAGON_BALL_ACTIVE = not DRAGON_BALL_ACTIVE end)

-- Render Row 2 Controls
buildSystemButton("⚔️ Equip: ON", "⚔️ Equip: OFF", 80, 82, "EQUIP_ACTIVE", function() EQUIP_ACTIVE = not EQUIP_ACTIVE end)
buildSystemButton("✨ Spells: ON", "✨ Spells: OFF", 225, 82, "SPELL_ACTIVE", function() SPELL_ACTIVE = not SPELL_ACTIVE end)

-- Tri-State List Renderer Engine
local function createCategoryScroll(titleText, xOffset, targetSubTable)
    local container = Instance.new("Frame")
    container.Size = UDim2.new(0, 205, 0, 220)
    container.Position = UDim2.new(0, xOffset, 0, 125)
    container.BackgroundTransparency = 1
    container.Parent = frame

    local catTitle = Instance.new("TextLabel")
    catTitle.Size = UDim2.new(1, 0, 0, 20)
    catTitle.BackgroundTransparency = 1
    catTitle.Text = titleText
    catTitle.TextColor3 = Color3.fromRGB(200, 200, 200)
    catTitle.Font = Enum.Font.SourceSansBold
    catTitle.TextSize = 14
    catTitle.Parent = container

    local scroll = Instance.new("ScrollingFrame")
    scroll.Size = UDim2.new(1, 0, 1, -25)
    scroll.Position = UDim2.new(0, 0, 0, 25)
    scroll.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
    scroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    scroll.ScrollBarThickness = 5
    scroll.Parent = container

    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 4)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.Parent = scroll

    local sortedNames = {}
    for name in pairs(targetSubTable) do table.insert(sortedNames, name) end
    table.sort(sortedNames)

    for _, mobName in ipairs(sortedNames) do
        local mobBtn = Instance.new("TextButton")
        mobBtn.Size = UDim2.new(0, 190, 0, 30)
        mobBtn.Font = Enum.Font.SourceSans
        mobBtn.TextSize = 13
        mobBtn.Parent = scroll

        local function syncVisuals()
            local state = targetSubTable[mobName]
            if state == "ON" then
                mobBtn.Text = mobName .. " [ON]"
                mobBtn.BackgroundColor3 = Color3.fromRGB(56, 92, 145)
                mobBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            elseif state == "PRIORITY" then
                mobBtn.Text = "⭐ " .. mobName .. " [PRIO]"
                mobBtn.BackgroundColor3 = Color3.fromRGB(168, 126, 42)
                mobBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            else
                mobBtn.Text = mobName .. " [OFF]"
                mobBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
                mobBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
            end
        end
        syncVisuals()

        mobBtn.MouseButton1Click:Connect(function()
            local current = targetSubTable[mobName]
            if current == "OFF" then targetSubTable[mobName] = "ON"
            elseif current == "ON" then targetSubTable[mobName] = "PRIORITY" currentTarget = nil
            else targetSubTable[mobName] = "OFF" currentTarget = nil end
            syncVisuals()
            saveConfig()
        end)
    end
    scroll.CanvasSize = UDim2.new(0, 0, 0, layout.AbsoluteContentSize.Y + 10)
end

createCategoryScroll("⚔️ Mobs List", 10, targets.Mobs)
createCategoryScroll("👹 Bosses & Elites", 225, targets.Bosses)

-- Slider Construction Factory Module
local function createSlider(axisName, minVal, maxVal, defaultStartVal, yOffset)
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -20, 0, 20)
    label.Position = UDim2.new(0, 10, 0, yOffset)
    label.BackgroundTransparency = 1
    local suffix = (axisName:find("Orbit") and "°") or " studs"
    label.TextColor3 = Color3.fromRGB(220, 220, 220)
    label.Font = Enum.Font.SourceSansBold
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local sliderBg = Instance.new("Frame")
    sliderBg.Size = UDim2.new(1, -20, 0, 8)
    sliderBg.Position = UDim2.new(0, 10, 0, yOffset + 20)
    sliderBg.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    sliderBg.BorderSizePixel = 0
    sliderBg.Parent = frame

    local sliderBtn = Instance.new("TextButton")
    sliderBtn.Size = UDim2.new(0, 16, 0, 16)
    sliderBtn.BackgroundColor3 = Color3.fromRGB(150, 150, 150)
    sliderBtn.Text = ""
    sliderBtn.BorderSizePixel = 0
    sliderBtn.Parent = sliderBg

    local function refreshSliderVisuals()
        local liveValue = defaultStartVal
        if axisName == "🔄 Orbit Position" then liveValue = currentOffset.X
        elseif axisName == "📐 Height" then liveValue = currentOffset.Y
        elseif axisName == "📏 Distance" then liveValue = currentOffset.Z end

        label.Text = axisName .. ": " .. tostring(liveValue) .. suffix
        local pct = (liveValue - minVal) / (maxVal - minVal)
        sliderBtn.Position = UDim2.new(pct, -8, 0.5, -8)
    end
    refreshSliderVisuals()

    local activeDragging = false
    local function updateSliderPosition(inputPosition)
        local barAbsPos = sliderBg.AbsolutePosition.X
        local barAbsSize = sliderBg.AbsoluteSize.X
        local clmp = math.clamp((inputPosition.X - barAbsPos) / barAbsSize, 0, 1)
        local value = math.round(minVal + (clmp * (maxVal - minVal)))
        
        if axisName == "🔄 Orbit Position" then currentOffset = Vector3.new(value, currentOffset.Y, currentOffset.Z)
        elseif axisName == "📐 Height" then currentOffset = Vector3.new(currentOffset.X, value, currentOffset.Z)
        elseif axisName == "📏 Distance" then currentOffset = Vector3.new(currentOffset.X, currentOffset.Y, value) end
        
        refreshSliderVisuals()
        saveConfig()
    end

    sliderBtn.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then activeDragging = true end end)
    UIS.InputChanged:Connect(function(input) if activeDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then updateSliderPosition(input.Position) end end)
    UIS.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then activeDragging = false end end)
end

createSlider("🔄 Orbit Position", -180, 180, 0, 360)
createSlider("📐 Height", -20, 30, 5, 410)
createSlider("📏 Distance", 0, 50, 15, 460)

-- Header Dragging Mechanics Panel
local dragging, dragStart, startPos = false, nil, nil
frame.InputBegan:Connect(function(input) if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) and input.Position.Y - frame.AbsolutePosition.Y <= 35 then dragging, dragStart, startPos = true, input.Position, frame.Position end end)
UIS.InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then local delta = input.Position - dragStart frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end)
UIS.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end)

-- Low-Overhead Event Scanner Pipeline Setup
local function initializeStreamingCache()
    local entities = workspace:WaitForChild("Entities")
    local eventsFolder = entities:WaitForChild("Events")
    
    local function processObject(descendant)
        if descendant:IsA("Model") or descendant:IsA("BasePart") then
            if descendant.Name == "Chest" then
                table.insert(activeChestsCache, descendant)
            elseif string.match(descendant.Name, "^Dragon Ball %d+$") then
                table.insert(activeDragonBallsCache, descendant)
            end
        end
    end

    eventsFolder.DescendantAdded:Connect(processObject)
    workspace.DescendantAdded:Connect(function(descendant)
        if string.match(descendant.Name, "^Dragon Ball %d+$") then table.insert(activeDragonBallsCache, descendant) end
    end)
    
    for _, item in ipairs(eventsFolder:GetDescendants()) do processObject(item) end
    for _, item in ipairs(workspace:GetChildren()) do if string.match(item.Name, "^Dragon Ball %d+$") then table.insert(activeDragonBallsCache, item) end end
end
task.spawn(initializeStreamingCache)

-- Prompt Target Evaluators
local function getActiveDragonBallPrompt()
    if not DRAGON_BALL_ACTIVE then return nil, nil end
    for i = #activeDragonBallsCache, 1, -1 do
        local obj = activeDragonBallsCache[i]
        if not obj or not obj.Parent then table.remove(activeDragonBallsCache, i) continue end
        
        local prompt = obj:FindFirstChildOfClass("ProximityPrompt") or obj:FindFirstChild("ProximityPrompt", true)
        local targetPart = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart", true)
        if prompt and targetPart then return targetPart, prompt end
    end
    return nil, nil
end

local function getActiveEventChestPrompt()
    if not CHEST_ACTIVE then return nil, nil, nil end
    local validChests = {}
    
    for i = #activeChestsCache, 1, -1 do
        local item = activeChestsCache[i]
        if not item or not item.Parent then
            chestVisitTimers[item] = nil 
            chestVisitCounts[item] = nil 
            table.remove(activeChestsCache, i)
            continue
        end

        if chestVisitCounts[item] and chestVisitCounts[item] >= 2 then continue end

        local creditedFolder = item:FindFirstChild("Credited")
        if creditedFolder then
            local isMyChest = false
            for _, credit in ipairs(creditedFolder:GetChildren()) do
                if string.find(string.lower(credit.Name), string.lower(player.Name), 1, true) then isMyChest = true break end
            end
            if not isMyChest then continue end
        end

        local prompt = item:FindFirstChildOfClass("ProximityPrompt") or item:FindFirstChild("ProximityPrompt", true)
        local targetPart = prompt and (prompt.Parent:IsA("BasePart") and prompt.Parent or item:FindFirstChild("middle") or item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart", true))
        if prompt and targetPart then table.insert(validChests, {prompt = prompt, model = item, part = targetPart}) end
    end

    if #validChests == 0 then currentChestIndex = nil return nil, nil, nil end

    if currentChestIndex and currentChestIndex <= #validChests then
        local activeSelection = validChests[currentChestIndex]
        local startTime = chestVisitTimers[activeSelection.model]
        if startTime and (os.clock() - startTime) < 3 then
            return activeSelection.prompt, activeSelection.model, activeSelection.part
        elseif startTime then
            chestVisitCounts[activeSelection.model] = (chestVisitCounts[activeSelection.model] or 0) + 1
            chestVisitTimers[activeSelection.model] = nil 
        end
    end

    local nextIndex = 1
    if currentChestIndex then nextIndex = (currentChestIndex % #validChests) + 1 end

    local loopsChecked = 0
    while loopsChecked < #validChests do
        local checkSelection = validChests[nextIndex]
        if not chestVisitCounts[checkSelection.model] or chestVisitCounts[checkSelection.model] < 2 then break end
        nextIndex = (nextIndex % #validChests) + 1
        loopsChecked = loopsChecked + 1
    end

    local finalSelection = validChests[nextIndex]
    if chestVisitCounts[finalSelection.model] and chestVisitCounts[finalSelection.model] >= 2 then currentChestIndex = nil return nil, nil, nil end

    currentChestIndex = nextIndex
    chestVisitTimers[finalSelection.model] = os.clock()
    return finalSelection.prompt, finalSelection.model, finalSelection.part
end

-- Dynamic asset cache tables
activeChestsCache = {}
activeDragonBallsCache = {}

-- Bulletproof streaming scanner completely replaces loop scans to stop lag [INDEX]
local function initializeStreamingCache()
    local entities = workspace:WaitForChild("Entities")
    local eventsFolder = entities:WaitForChild("Events")
    
    local function processObject(descendant)
        if descendant:IsA("Model") or descendant:IsA("BasePart") then
            if descendant.Name == "Chest" then
                table.insert(activeChestsCache, descendant)
            elseif string.match(descendant.Name, "^Dragon Ball %d+$") then
                table.insert(activeDragonBallsCache, descendant)
            end
        end
    end

    -- Watch assets entry point natively via engine connection events [INDEX]
    eventsFolder.DescendantAdded:Connect(processObject)
    workspace.DescendantAdded:Connect(function(descendant)
        if string.match(descendant.Name, "^Dragon Ball %d+$") then
            table.insert(activeDragonBallsCache, descendant)
        end
    end)
    
    -- Fast initial load sweep (runs once only on script execution) [INDEX]
    for _, item in ipairs(eventsFolder:GetDescendants()) do processObject(item) end
    for _, item in ipairs(workspace:GetChildren()) do 
        if string.match(item.Name, "^Dragon Ball %d+$") then 
            table.insert(activeDragonBallsCache, item) 
        end 
    end
end
task.spawn(initializeStreamingCache)

-- Universal Dragon Ball Detector (Absolute Highest Override Priority Layer) [INDEX]
local function getActiveDragonBallPrompt()
    for i = #activeDragonBallsCache, 1, -1 do
        local obj = activeDragonBallsCache[i]
        if not obj or not obj.Parent then
            table.remove(activeDragonBallsCache, i)
            continue
        end
        local prompt = obj:FindFirstChildOfClass("ProximityPrompt") or obj:FindFirstChild("ProximityPrompt", true)
        local targetPart = obj:IsA("BasePart") and obj or obj:FindFirstChildWhichIsA("BasePart", true)
        if prompt and targetPart then
            return targetPart, prompt
        end
    end
    return nil, nil
end

-- Round-Robin Event Chest Selector Engine (3-Second Timed Loops) [INDEX]
local function getActiveEventChestPrompt()
    if not CHEST_ACTIVE then return nil, nil, nil end
    local validChests = {}
    
    -- 1. Parse cache and filter out chests that have already completed 2 rounds [INDEX]
    for i = #activeChestsCache, 1, -1 do
        local item = activeChestsCache[i]
        if not item or not item.Parent then
            chestVisitTimers[item] = nil 
            chestVisitCounts[item] = nil 
            table.remove(activeChestsCache, i)
            continue
        end

        if chestVisitCounts[item] and chestVisitCounts[item] >= 2 then continue end

        -- Whitelisted Credit-Checker verification block [INDEX]
        local creditedFolder = item:FindFirstChild("Credited")
        if creditedFolder then
            local isMyChest = false
            for _, credit in ipairs(creditedFolder:GetChildren()) do
                if string.find(string.lower(credit.Name), string.lower(player.Name), 1, true) then 
                    isMyChest = true 
                    break 
                end
            end
            if not isMyChest then continue end
        end

        local prompt = item:FindFirstChildOfClass("ProximityPrompt") or item:FindFirstChild("ProximityPrompt", true)
        local targetPart = prompt and (prompt.Parent:IsA("BasePart") and prompt.Parent or item:FindFirstChild("middle") or item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart", true))
        
        if prompt and targetPart then
            table.insert(validChests, {prompt = prompt, model = item, part = targetPart})
        end
    end

    if #validChests == 0 then currentChestIndex = nil return nil, nil, nil end

    -- 2. Verify if our active selection is still within its 3-second loop window [INDEX]
    if currentChestIndex and currentChestIndex <= #validChests then
        local activeSelection = validChests[currentChestIndex]
        local startTime = chestVisitTimers[activeSelection.model]
        if startTime and (os.clock() - startTime) < 3 then
            return activeSelection.prompt, activeSelection.model, activeSelection.part
        elseif startTime then
            -- Time limit hit! Finalize round and increment visit array [INDEX]
            chestVisitCounts[activeSelection.model] = (chestVisitCounts[activeSelection.model] or 0) + 1
            chestVisitTimers[activeSelection.model] = nil 
        end
    end

    local nextIndex = 1
    if currentChestIndex then nextIndex = (currentChestIndex % #validChests) + 1 end

    local loopsChecked = 0
    while loopsChecked < #validChests do
        local checkSelection = validChests[nextIndex]
        if not chestVisitCounts[checkSelection.model] or chestVisitCounts[checkSelection.model] < 2 then break end
        nextIndex = (nextIndex % #validChests) + 1
        loopsChecked = loopsChecked + 1
    end

    local finalSelection = validChests[nextIndex]
    if chestVisitCounts[finalSelection.model] and chestVisitCounts[finalSelection.model] >= 2 then 
        currentChestIndex = nil 
        return nil, nil, nil 
    end

    currentChestIndex = nextIndex
    chestVisitTimers[finalSelection.model] = os.clock()
    return finalSelection.prompt, finalSelection.model, finalSelection.part
end

-- Hand-Mapped Precise Spawn Directory (Matches your exact game paths) [INDEX]
local spawnMap = {
    ["Vennum"] = "VennumSpawn",
    ["Alpha Wolf"] = "AlphaWolfSpawn",
    ["Warlord"] = "AlphaWolfSpawn",
    ["Terra"] = "StoneGuardian1",
    ["Red"] = "VampireSoldierSpawn",
    ["Alpha Desert Claw"] = "DesertClaw1",
    ["Apex"] = "AlphaWolfSpawn",
    ["Kraken"] = "Desert Kraken",
    ["The Crimson Witch"] = "WindMageSpawn",
    ["Netherwing"] = "LesserDevilSpawn",
    ["Khamsin"] = "DesertClaw1",
    ["Ice Golem"] = "YettiSpawn1",
    ["Nyxar"] = "DarkSkeleton",
    ["Word Devil"] = "LesserDevilSpawn",
    ["Devil"] = "LesserDevilSpawn",
    ["Medusa"] = "OgreSpawn",
    ["Aetherion"] = "LesserDevilSpawn",
    ["Morningstar"] = "LesserDevilSpawn",
    ["Primal Werewolf"] = "WerewolfSoldierSpawn",
    ["Elder Vampire"] = "VampireSoldierSpawn",
    ["Yeti"] = "YettiSpawn1",
    ["Vampire Soldier"] = "VampireSoldierSpawn",
    ["Stone Guardian"] = "StoneGuardian1",
    ["Shadow Hand Goon"] = "ShadowHandSpawn1",
    ["Seasoned Mercenary"] = "SeasonedMercenarySpawn",
    ["Riddled Skeleton Soldier"] = "DarkSkeleton",
    ["Pumpkin"] = "Pumpkins",
    ["Wolf"] = "WoldSpawn",
    ["Mage Apprentice"] = "FireMageSpawn",
    ["Lesser Devil Juggernaut"] = "LesserDevilSpawn",
    ["Desert Claw"] = "DesertClaw1",
    ["Bandit"] = "Bandit2Spawn",
    ["Wolf Hunter"] = "WoldSpawn",
    ["Wind Monk"] = "WindMageSpawn",
    ["Lowly Goblin"] = "LowlyGoblinn",
    ["Werewolf Soldier"] = "WerewolfSoldierSpawn"
}

-- Multi-Layer Target Selection Evaluator (Partial String Matches) [INDEX]
local function getActiveFarmTarget()
    local entities = workspace:FindFirstChild("Entities")
    local enemiesFolder = entities and entities:FindFirstChild("Enemies")
    if not enemiesFolder then currentTarget = nil return nil end
    local enemiesList = enemiesFolder:GetChildren()

    -- LAYER 1: Starred Custom Priority Selections
    for _, mob in ipairs(enemiesList) do
        local mobRoot = mob:FindFirstChild("HumanoidRootPart")
        local mobHum = mob:FindFirstChildOfClass("Humanoid")
        if mobRoot and mobHum and mobHum.Health > 0 then
            local lowerMobName = string.lower(mob.Name)
            for name, state in pairs(targets.Bosses) do if state == "PRIORITY" and string.find(lowerMobName, string.lower(name), 1, true) then currentTarget = mob return currentTarget end end
            for name, state in pairs(targets.Mobs) do if state == "PRIORITY" and string.find(lowerMobName, string.lower(name), 1, true) then currentTarget = mob return currentTarget end end
        end
    end

    -- LAYER 2: Regular Enabled Bosses
    for _, mob in ipairs(enemiesList) do
        local mobRoot = mob:FindFirstChild("HumanoidRootPart")
        local mobHum = mob:FindFirstChildOfClass("Humanoid")
        if mobRoot and mobHum and mobHum.Health > 0 then
            local lowerMobName = string.lower(mob.Name)
            for name, state in pairs(targets.Bosses) do if state == "ON" and string.find(lowerMobName, string.lower(name), 1, true) then currentTarget = mob return currentTarget end end
        end
    end

    -- LAYER 3: Sticky Check Target Memory Locking
    if currentTarget and currentTarget.Parent == enemiesFolder then
        local mobRoot = currentTarget:FindFirstChild("HumanoidRootPart")
        local mobHum = currentTarget:FindFirstChildOfClass("Humanoid")
        if mobRoot and mobHum and mobHum.Health > 0 then
            local currentLower = string.lower(currentTarget.Name)
            local isStillEnabled = false
            for name, state in pairs(targets.Mobs) do if state == "ON" and string.find(currentLower, string.lower(name), 1, true) then isStillEnabled = true break end end
            for name, state in pairs(targets.Bosses) do if state == "ON" and string.find(currentLower, string.lower(name), 1, true) then isStillEnabled = true break end end
            if isStillEnabled then return currentTarget end
        end
    end

    -- LAYER 4: Standard Enabled Mobs Loop
    currentTarget = nil
    for _, mob in ipairs(enemiesList) do
        local mobRoot = mob:FindFirstChild("HumanoidRootPart")
        local mobHum = mob:FindFirstChildOfClass("Humanoid")
        if mobRoot and mobHum and mobHum.Health > 0 then
            local lowerMobName = string.lower(mob.Name)
            for name, state in pairs(targets.Mobs) do if state == "ON" and string.find(lowerMobName, string.lower(name), 1, true) then currentTarget = mob return currentTarget end end
        end
    end
    return nil
end

-- Zero-Delay Static Spawn Pad Teleporter [INDEX]
local function getActiveSpawnVicinity()
    local entities = workspace:FindFirstChild("Entities")
    local spawnsFolder = entities and entities:FindFirstChild("Spawns")
    if not spawnsFolder then return nil end

    -- 1. Check your marked PRIORITY pads first
    for name, state in pairs(targets.Bosses) do
        if state == "PRIORITY" then
            local padName = spawnMap[name]
            local pad = padName and spawnsFolder:FindFirstChild(padName)
            if pad then return pad end
        end
    end
    for name, state in pairs(targets.Mobs) do
        if state == "PRIORITY" then
            local padName = spawnMap[name]
            local pad = padName and spawnsFolder:FindFirstChild(padName)
            if pad then return pad end
        end
    end

    -- 2. Fall back to standard enabled targets if no priority pads match
    for name, state in pairs(targets.Bosses) do
        if state == "ON" then
            local padName = spawnMap[name]
            local pad = padName and spawnsFolder:FindFirstChild(padName)
            if pad then return pad end
        end
    end
    for name, state in pairs(targets.Mobs) do
        if state == "ON" then
            local padName = spawnMap[name]
            local pad = padName and spawnsFolder:FindFirstChild(padName)
            if pad then return pad end
        end
    end

    return nil
end

RunService.RenderStepped:Connect(function()
    local character = player.Character
    local myRoot = character and character:FindFirstChild("HumanoidRootPart")
    local myHum = character and character:FindFirstChildOfClass("Humanoid")
    
    -- Cleanup parameters: safely remove flight values if dead or turned off
    if not myRoot or (myHum and myHum.Health <= 0) or not FARMING_ACTIVE then 
        if flyAnchor then flyAnchor:Destroy() flyAnchor = nil end
        currentTarget = nil
        return 
    end

    -- 🚨 PRIORITY 1: Dragon Ball Hard-Lock (Instantly drops all other tasks)
    if DRAGON_BALL_ACTIVE then
        local dbPart, dbPrompt = getActiveDragonBallPrompt()
        if dbPart and dbPrompt then
            if not flyAnchor or flyAnchor.Parent ~= myRoot then
                if flyAnchor then flyAnchor:Destroy() end
                flyAnchor = Instance.new("BodyVelocity")
                flyAnchor.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                flyAnchor.Velocity = Vector3.new(0, 0, 0)
                flyAnchor.Parent = myRoot
            end
            myRoot.CFrame = dbPart.CFrame * CFrame.new(0, 3, 0)
            return
        end
    end

    -- 🎁 PRIORITY 2: Event Chest Collection Carousel (Runs if no Dragon Balls exist)
    local chestPrompt, chestModel, chestMiddlePart = getActiveEventChestPrompt()
    if chestPrompt and chestModel and chestMiddlePart then
        if not flyAnchor or flyAnchor.Parent ~= myRoot then
            if flyAnchor then flyAnchor:Destroy() end
            flyAnchor = Instance.new("BodyVelocity")
            flyAnchor.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            flyAnchor.Velocity = Vector3.new(0, 0, 0)
            flyAnchor.Parent = myRoot
        end
        myRoot.CFrame = chestMiddlePart.CFrame * CFrame.new(0, 2, 0)
        return
    end

    -- ⚔️ PRIORITY 3: Combat Target Alignment & Orbital Flying Engine
    local currentEnemy = getActiveFarmTarget()
    if currentEnemy and currentEnemy:FindFirstChild("HumanoidRootPart") then
        local enemyRoot = currentEnemy.HumanoidRootPart
        
        if not flyAnchor or flyAnchor.Parent ~= myRoot then
            if flyAnchor then flyAnchor:Destroy() end
            flyAnchor = Instance.new("BodyVelocity")
            flyAnchor.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            flyAnchor.Velocity = Vector3.new(0, 0, 0)
            flyAnchor.Parent = myRoot
        end
        
        -- Front-To-Back Vertical Orbit calculations using your slider layout offsets
        local angleRad = math.rad(currentOffset.X) 
        local orbitY = math.sin(angleRad) * currentOffset.Z + currentOffset.Y
        local orbitZ = math.cos(angleRad) * currentOffset.Z
        
        local worldOffsetPosition = (enemyRoot.CFrame * CFrame.new(0, orbitY, orbitZ)).Position
        myRoot.CFrame = CFrame.lookAt(worldOffsetPosition, enemyRoot.Position)
        return
    end

    -- 🌌 PRIORITY 4: Zone Pre-Teleport Latch (Hovering safely over static spawn pad)
    local spawnPart = getActiveSpawnVicinity()
    if spawnPart then
        if not flyAnchor or flyAnchor.Parent ~= myRoot then
            if flyAnchor then flyAnchor:Destroy() end
            flyAnchor = Instance.new("BodyVelocity")
            flyAnchor.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            flyAnchor.Velocity = Vector3.new(0, 0, 0)
            flyAnchor.Parent = myRoot
        end
        -- Hovers 12 studs directly above spawn pad to allow triggers to safely map coordinates
        myRoot.CFrame = spawnPart.CFrame * CFrame.new(0, 12, 0)
    else
        -- Safely strip constraints down if nothing is found active
        if flyAnchor then flyAnchor:Destroy() flyAnchor = nil end
    end
end)

-- Loop 1: Core Collectible Engine Thread (Fired smoothly on Engine Ticks)
task.spawn(function()
    while true do
        RunService.Heartbeat:Wait()
        if FARMING_ACTIVE then
            pcall(function()
                if DRAGON_BALL_ACTIVE then
                    local dbPart, dbPrompt = getActiveDragonBallPrompt()
                    if dbPrompt then 
                        fireproximityprompt(dbPrompt) 
                        return 
                    end
                end

                local chestPrompt = getActiveEventChestPrompt()
                if chestPrompt then 
                    fireproximityprompt(chestPrompt) 
                end
            end)
        end
    end
end)

-- Loop 2: Inventory Maintenance (Auto-Equips Weapons and Grimoire Books)
task.spawn(function()
    while true do
        task.wait(0.8)
        if FARMING_ACTIVE and EQUIP_ACTIVE then 
            pcall(function()
                local entitiesFolder = workspace:FindFirstChild("Entities")
                local entity = entitiesFolder and entitiesFolder:FindFirstChild(player.Name)
                
                -- Proximity fallback matching layer if index name switches
                if not entity then
                    local character = player.Character
                    local myRoot = character and character:FindFirstChild("HumanoidRootPart")
                    if myRoot then
                        for _, folderChild in ipairs(entitiesFolder:GetChildren()) do
                            local root = folderChild:FindFirstChild("HumanoidRootPart")
                            if root and (root.Position - myRoot.Position).Magnitude  0 and backpack then
                    for _, tool in ipairs(backpack:GetChildren()) do
                        if tool:IsA("Tool") and tool:GetAttribute("Type") == "Spell" then
                            tool.Parent = character
                            task.wait(0.05)
                        end
                    end
                end
            end)
        end
    end
end)

-- Loop 4: Anti-Idle Focus simulation listener module hook
local VirtualUser = game:GetService("VirtualUser")
player.Idled:Connect(function()
    pcall(function()
        VirtualUser:Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
        task.wait(1)
        VirtualUser:Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
    end)
end)

print("Master Integrated Automation Panel Fully Initialized!")
